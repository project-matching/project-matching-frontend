import styled from '@emotion/styled';

const Div = styled.div`
  font-size: 24px;
  font-weight: bold;
  padding-bottom: 5px;
`;

const Logo = () => {
  return <Div>Logo</Div>;
};

export default Logo;
